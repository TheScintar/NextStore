import React from 'react';



export const metadata = {
    title: 'Authentication',
    description: 'Login or registration account',
  };

const Layout = ({ children }) => {
  return (
    <>
    <main>{children}</main>
    </>
       
  );
};

export default Layout;
